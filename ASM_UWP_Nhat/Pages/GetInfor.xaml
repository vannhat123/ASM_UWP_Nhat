<Page
    x:Class="ASM_UWP_Nhat.Pages.GetInfor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ASM_UWP_Nhat.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:entity="using:ASM_UWP_Nhat.Entity"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ListView ItemsSource="{x:Bind listSongs}" x:Name="MyListSong" >
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="entity:ListSong1">
                    <StackPanel Margin="0, 10, 0, 10" Orientation="Horizontal"  Tag="{Binding}">
                        <Ellipse Width="77" Height="77">
                            <Ellipse.Fill>
                                <ImageBrush ImageSource="{x:Bind thumbnail}"/>
                            </Ellipse.Fill>
                        </Ellipse>
                        <StackPanel Margin="10, 10, 0, 0">
                            <TextBlock Text="{x:Bind name}"/>
                            <TextBlock Text="{x:Bind singer}"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
